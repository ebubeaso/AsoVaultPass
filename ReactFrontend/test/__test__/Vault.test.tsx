import React from 'react';
import { render, fireEvent } from '@testing-library/react';
import { VaultHome } from '../../src/Vault';

it.todo("render without crashing", () => {
    render(<VaultHome/>)
})